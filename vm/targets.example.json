{
  "targets": {
    "local-docker": {
      "transport": "local",
      "runtime": "docker"
    },
    "local-podman": {
      "transport": "local",
      "runtime": "podman"
    },
    "remote-docker": {
      "transport": "ssh",
      "ssh_host": "builder.example.com",
      "ssh_user": "builder",
      "ssh_port": 22,
      "runtime": "docker",
      "workspace_root": "/tmp/gaia-ecs-runs"
    }
  }
}
